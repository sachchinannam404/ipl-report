
@page
@model myapp.Pages.TeamsModel

<h1>Teams</h1>

<table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Coach</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var team in Model.Teams)
        {
            <tr>
                <td>@team.Name</td>
                <td>@team.Coach</td>
            </tr>
        }
    </tbody>
</table>
